#!/usr/bin/env sh
set -eu

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"

# Run Bun inside a Docker container with the repository mounted at /app.
exec docker run --rm -it \
  -v "$SCRIPT_DIR:/app" \
  -w /app \
  oven/bun:1 \
  bun "$@"
